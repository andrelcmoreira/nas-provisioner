---

- name: Sync package database
  become: true
  become_method: su
  apt:
    update_cache: yes

- name: Install packages
  become: true
  become_method: su
  apt:
    name: "{{ pkgs }}"
    state: present

...
